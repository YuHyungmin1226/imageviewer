# ImageViewer 의존성 패키지

# 필수 패키지
Pillow>=9.0.0

# 개발/빌드용 패키지 (선택사항)
pyinstaller>=5.0.0

# 타입 힌트 지원 (Python 3.5+ 기본 포함)
# typing 모듈은 Python 3.5+에서 기본으로 제공됨

# 추가 개발 도구 (선택사항)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0
# mypy>=1.0.0 