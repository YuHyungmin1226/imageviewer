# ImageViewer 의존성 패키지

# 필수 패키지
# 9.1.0 미만에는 코드가 사용하는 Image.Resampling.LANCZOS가 없어 AttributeError가 발생함
Pillow>=9.1.0

# 드래그 앤 드롭 지원 (선택사항)
# 설치하지 않아도 동작하며, 설치 시 창에 이미지 파일을 끌어다 놓을 수 있음
tkinterdnd2>=0.3.0

# 개발/빌드용 패키지 (선택사항)
pyinstaller>=5.0.0

# 타입 힌트 지원 (Python 3.5+ 기본 포함)
# typing 모듈은 Python 3.5+에서 기본으로 제공됨

# 추가 개발 도구 (선택사항)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0
# mypy>=1.0.0 